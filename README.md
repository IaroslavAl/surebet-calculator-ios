# Surebet Calculator

<p align="center">
  <img src="SurebetCalculator.gif" alt="Surebet Calculator Demo" width="250" />
</p>

## 📱 Описание

**Surebet Calculator** — это iOS-приложение для расчёта сурбетов (вилок) в ставках. Приложение позволяет быстро и точно рассчитать оптимальные размеры ставок на несколько исходов одного события, чтобы гарантировать прибыль независимо от результата.

Приложение разработано с использованием Swift 6 и SwiftUI, следует принципам SOLID и современным практикам iOS-разработки.

## ✨ Функциональные возможности

### Калькулятор сурбетов
- **Поддержка 2-10 ставок** — гибкая настройка количества исходов для расчёта
- **Автоматический расчёт размеров ставок** — приложение автоматически вычисляет оптимальные размеры ставок для каждого исхода
- **Расчёт прибыли** — показывает процент прибыли и доход для каждого исхода
- **Различные методы расчёта:**
  - По итоговой ставке — введите общий размер ставки, и приложение распределит его между исходами
  - По отдельным ставкам — введите размеры ставок для конкретных исходов, и приложение пересчитает остальные
  - Комбинированный режим — автоматическое переключение между методами в зависимости от ввода

### Адаптивный интерфейс
- **Поддержка iPhone и iPad** — оптимизированный интерфейс для разных размеров экранов
- **Тёмная тема** — современный дизайн с поддержкой тёмной темы
- **Интуитивная навигация** — простой и понятный интерфейс

### Дополнительные возможности
- **Онбординг для новых пользователей** — интерактивное руководство по использованию приложения
- **Система баннеров** — интеграция рекламных баннеров
- **Запрос отзывов** — автоматический запрос отзывов пользователей в App Store
- **Аналитика** — интеграция с AppMetrica для отслеживания использования приложения

## 📋 Требования

- **iOS:** 16.0 и выше
- **Swift:** 6.0
- **Xcode:** 15.0 и выше (рекомендуется последняя версия)
- **Устройства:** iPhone и iPad

## 🚀 Установка и сборка

### Предварительные требования

1. Убедитесь, что у вас установлен Xcode 15.0 или новее
2. Убедитесь, что у вас установлен Swift 6.0

### Установка зависимостей

Проект использует Swift Package Manager для управления зависимостями. Зависимости будут автоматически загружены при открытии проекта в Xcode.

Если нужно обновить зависимости вручную:

```bash
xcodebuild -resolvePackageDependencies -project surebet-calculator.xcodeproj
```

### Сборка проекта

1. Откройте `surebet-calculator.xcodeproj` в Xcode
2. Выберите схему `surebet-calculator`
3. Выберите симулятор или устройство для запуска
4. Нажмите `Cmd + B` для сборки или `Cmd + R` для запуска

### Запуск тестов

Для запуска всех тестов:

```bash
xcodebuild test -project surebet-calculator.xcodeproj -scheme surebet-calculator -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9'
```

## 🏗️ Архитектура проекта

Проект использует **модульную архитектуру** на основе Swift Package Manager. Каждый модуль отвечает за определённую функциональность и может быть переиспользован в других проектах.

### Структура модулей

```
SurebetCalculatorPackage/
├── AnalyticsManager/      # Управление аналитикой (AppMetrica)
├── Banner/                # Система баннеров и рекламы
├── Onboarding/            # Онбординг для новых пользователей
├── ReviewHandler/         # Запрос отзывов пользователей
├── Root/                  # Корневой модуль приложения
└── SurebetCalculator/     # Основной модуль калькулятора
```

### Принципы архитектуры

- **SOLID принципы:**
  - **Single Responsibility** — каждый модуль и компонент отвечает за одну задачу
  - **Open/Closed** — код открыт для расширения через протоколы, закрыт для модификации
  - **Dependency Inversion** — зависимости внедряются через инициализаторы, используются протоколы для инверсии зависимостей

- **Swift 6 Concurrency:**
  - Использование `async/await` для асинхронных операций
  - `@MainActor` для всех ViewModel и UI-компонентов
  - `Sendable` конформность для всех моделей данных

- **Dependency Injection:**
  - Все сервисы внедряются через инициализаторы
  - Использование протоколов для абстракции зависимостей
  - Легкое тестирование через моки

### Основные компоненты

- **ViewModels** — управление состоянием UI, помечены `@MainActor`
- **Services** — бизнес-логика и вычисления (CalculationService, AnalyticsService, BannerService, ReviewService)
- **Views** — SwiftUI компоненты для отображения UI
- **Models** — структуры данных с `Sendable` конформностью

## 📦 Зависимости

Проект использует следующие внешние зависимости:

- **[AppMetrica SDK](https://github.com/appmetrica/appmetrica-sdk-ios)** (5.11.0+) — аналитика и отслеживание событий
- **[SwiftLint](https://github.com/realm/SwiftLint)** (0.59.1+) — статический анализатор кода
- **[SDWebImageSwiftUI](https://github.com/SDWebImage/SDWebImageSwiftUI)** (3.1.3+) — загрузка и кэширование изображений для баннеров

## 🧪 Тестирование

Проект имеет комплексное покрытие тестами для обеспечения стабильности и качества кода.

### ✅ Unit Tests (XCTest)

Бизнес-логика и сервисы покрыты модульными тестами:

| Модуль | Описание |
|--------|----------|
| `SurebetCalculatorTests` | Калькулятор, ViewModel, Extensions, CalculationService |
| `OnboardingTests` | OnboardingViewModel и edge cases |
| `RootTests` | RootViewModel, интеграционные тесты |
| `AnalyticsManagerTests` | AnalyticsManager и AnalyticsService |
| `BannerTests` | BannerService (fetch, cache, network) |
| `ReviewHandlerTests` | ReviewService и ReviewHandler |

**Покрытие:**
- ✅ Core Logic: Calculator, Extensions (`Double.formatToString()`, `String.formatToDouble()`)
- ✅ ViewModels: SurebetCalculatorViewModel, OnboardingViewModel, RootViewModel
- ✅ Services: CalculationService, AnalyticsService, BannerService, ReviewService
- ✅ Edge Cases: нулевые/отрицательные значения, overflow protection, concurrency

### ✅ UI Tests (XCUITest)

Основные сценарии автоматизированы:

- ✅ **Happy Path:** запуск приложения, онбординг, ввод данных, расчёт сурбета
- ✅ **Edge Cases:** невалидные данные, очистка полей, изменение количества строк

### Запуск тестов

**В Xcode:**
- `Cmd + U` — запуск всех тестов

**Из командной строки:**

```bash
# Все тесты
xcodebuild test -project surebet-calculator.xcodeproj -scheme surebet-calculator -destination 'platform=iOS Simulator,name=iPhone 16'

# Только Unit тесты
xcodebuild test -project surebet-calculator.xcodeproj -scheme surebet-calculator -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SurebetCalculatorPackageTests

# Только UI тесты
xcodebuild test -project surebet-calculator.xcodeproj -scheme surebet-calculator -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SurebetCalculatorUITests
```

### Принципы тестирования

- **Dependency Injection:** все сервисы внедряются через инициализаторы для лёгкого мокирования
- **Hand-written Mocks:** используются ручные моки вместо библиотек
- **Swift 6 Concurrency:** `@MainActor` isolation для ViewModels, `Sendable` конформность для моков
- **Given-When-Then:** структура тестов для улучшения читаемости

## 📝 Локализация

Приложение поддерживает локализацию через String Catalogs (`.xcstrings`). Все строки в UI вынесены в файлы локализации:

- `SurebetCalculatorPackage/Sources/SurebetCalculator/Resources/Localizable.xcstrings`
- `SurebetCalculatorPackage/Sources/Onboarding/Resources/Localizable.xcstrings`
- `SurebetCalculatorPackage/Sources/Root/Resources/Localizable.xcstrings`

## 🔧 Разработка

### Стандарты кода

- **Swift 6** — использование современных возможностей языка
- **SwiftLint** — автоматическая проверка стиля кода
- **MARK разделы** — организация кода с помощью `// MARK: - Section Name`
- **Документация** — все публичные API документируются на русском языке с помощью Swift Doc (`///`)

### Правила коммитов

Проект использует [Conventional Commits](https://www.conventionalcommits.org/) на русском языке:

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `refactor:` — рефакторинг без изменения функциональности
- `docs:` — изменения в документации
- `test:` — добавление или изменение тестов

Пример: `feat(calculator): добавил поддержку 10 ставок`

## 📸 Скриншоты

Демонстрация работы приложения доступна в файле `SurebetCalculator.gif` в корне проекта.

## 📄 Версия

Текущая версия приложения: **1.7.0**

## 👤 Автор

Разработано для iOS с использованием Swift 6 и SwiftUI.

---

**Примечание:** Это приложение предназначено для образовательных целей и демонстрации расчёта сурбетов. Использование в реальных ставках осуществляется на свой страх и риск.
