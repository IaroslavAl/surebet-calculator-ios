import SwiftUI

public extension DesignSystem {
    /// Стандартные размеры UI-элементов.
    enum Size {
        /// Компактная высота контрола (40pt).
        public static let controlCompactHeight: CGFloat = 40
        /// Обычная высота контрола (60pt).
        public static let controlRegularHeight: CGFloat = 60
        /// Высота тулбара над клавиатурой (44pt).
        public static let keyboardAccessoryToolbarHeight: CGFloat = 44
    }
}
