# 📋 Roadmap документации для System Context

> **Цель:** Создать исчерпывающую базу знаний (System Context) для LLM-агентов, работающих с проектом SurebetCalculator.

**Статус:** 🚧 В работе  
**Ветка:** `docs/architecture-roadmap`  
**Последнее обновление:** 2026-01-25

---

## 📊 Общий прогресс

| Раздел | Статус | Прогресс |
|--------|--------|----------|
| 1. Architecture | ✅ Готово | 4/4 |
| 2. Tech Stack | ✅ Готово | 3/3 |
| 3. Rules | 🚧 В работе | 2/4 |
| 4. Data Flow | ⬜ Не начато | 0/4 |
| 5. Testing | ⬜ Не начато | 0/2 |

**Общий прогресс:** 9/17 задач (53%)

---

## 1. Architecture (Архитектура)

### 1.1. Модульная структура (SPM)
- [x] Документировать граф зависимостей модулей
- [x] Описать public API каждого модуля
- [x] Добавить диаграмму зависимостей (Mermaid)

**Зачем AI-агенту:** Понимание границ модулей позволяет агенту правильно размещать новый код, избегать циклических зависимостей и импортировать только необходимые модули.

| Модуль | Зависимости | Назначение |
|--------|-------------|------------|
| `Root` | `AnalyticsManager`, `Banner`, `Onboarding`, `ReviewHandler`, `SurebetCalculator` | Entry point, координация модулей |
| `SurebetCalculator` | `Banner` | Основная бизнес-логика калькулятора |
| `Banner` | `AnalyticsManager`, `SDWebImageSwiftUI` | Система баннеров |
| `Onboarding` | — | Онбординг новых пользователей |
| `ReviewHandler` | — | Запрос отзывов |
| `AnalyticsManager` | `AppMetricaCore` | Аналитика |

---

### 1.2. Паттерн MVVM
- [x] Документировать роли View, ViewModel, Service
- [x] Привести примеры кода для каждого слоя
- [x] Описать правила взаимодействия между слоями

**Зачем AI-агенту:** Знание паттерна позволяет агенту правильно распределять логику между View, ViewModel и Service, не нарушая Single Responsibility.

```
View (SwiftUI) → ViewModel (@MainActor, ObservableObject) → Service (Protocol)
```

---

### 1.3. Dependency Injection
- [x] Документировать паттерн DI через init
- [x] Привести примеры создания и внедрения сервисов
- [x] Описать протоколы сервисов

**Зачем AI-агенту:** Понимание DI через init позволяет агенту правильно внедрять зависимости и создавать тестируемый код с моками.

```swift
init(
    analyticsService: AnalyticsService = AnalyticsManager(),
    reviewService: ReviewService = ReviewHandler()
)
```

---

### 1.4. Навигация
- [x] Документировать навигационную структуру
- [x] Описать условный рендеринг экранов
- [x] Документировать модальные overlay

**Зачем AI-агенту:** Понимание навигационной структуры позволяет агенту добавлять новые экраны без нарушения существующего flow.

- `RootView` → условный рендеринг `OnboardingView` / `SurebetCalculatorView`
- `NavigationView` с `.stack` стилем
- Модальные overlay для fullscreen баннеров

---

## 2. Tech Stack (Технологический стек)

### 2.1. Версии и платформы
- [x] Документировать все версии (Swift, iOS, Xcode)
- [x] Описать минимальные требования
- [x] Добавить информацию о симуляторах для тестов

**Зачем AI-агенту:** Знание версий позволяет агенту использовать актуальные API и избегать deprecated методов.

| Компонент | Версия |
|-----------|--------|
| Swift | 6.0 |
| iOS Deployment Target | 16.0+ |
| Xcode | 15.0+ |
| App Version | 1.7.0 |

---

### 2.2. Внешние зависимости
- [x] Документировать все зависимости с примерами использования
- [x] Описать API каждой библиотеки
- [x] Добавить ссылки на документацию

**Зачем AI-агенту:** Понимание доступных библиотек позволяет агенту использовать их API вместо написания велосипедов.

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| AppMetrica SDK | 5.11.0+ | Аналитика событий |
| SwiftLint | 0.59.1+ | Статический анализ кода |
| SDWebImageSwiftUI | 3.1.3+ | Загрузка/кэширование изображений |

---

### 2.3. Swift 6 Concurrency
- [x] Документировать правила использования @MainActor
- [x] Описать Sendable конформность
- [x] Привести примеры async/await
- [x] Документировать известные workarounds (UIDevice)

**Зачем AI-агенту:** Понимание модели конкурентности позволяет агенту писать потокобезопасный код без data races.

- `@MainActor` для всех ViewModel и UI-компонентов
- `Sendable` конформность для всех моделей и сервисов
- `async/await` для асинхронных операций
- `nonisolated(unsafe)` только для UIDevice workaround

---

## 3. Rules (Правила и стандарты)

### 3.1. Структура файлов
- [x] Документировать структуру папок модуля
- [x] Описать назначение каждой папки
- [x] Привести примеры организации кода

**Зачем AI-агенту:** Знание структуры позволяет агенту генерировать код, который соответствует существующему стилю проекта.

```
Sources/
└── ModuleName/
    ├── Models/
    ├── ViewModels/
    ├── Views/
    │   ├── Buttons/
    │   └── Components/
    ├── Resources/
    │   ├── Assets.xcassets/
    │   └── Localizable.xcstrings
    └── ModuleName.swift  // Public API (enum с static func view())
```

---

### 3.2. Нейминг и код-стайл
- [x] Документировать правила нейминга
- [x] Описать использование MARK секций
- [x] Привести примеры правильного форматирования
- [x] Документировать правила документирования (Swift Doc)

**Зачем AI-агенту:** Соблюдение нейминга обеспечивает консистентность кодовой базы.

- Модификаторы доступа: явно указывать `private`, `public`
- `MARK: - Section Name` для разделения секций
- Порядок: Properties → init → body → Public Methods → Private Methods
- Документация на **русском языке** в формате Swift Doc (`///`)

---

### 3.3. Git Conventions
- [ ] Документировать формат веток
- [ ] Описать формат коммитов (Conventional Commits)
- [ ] Привести примеры правильных и неправильных названий

**Зачем AI-агенту:** Знание конвенций позволяет агенту генерировать корректные commit messages и branch names.

- **Ветки:** `<тип>/<описание-через-дефисы>` (lowercase)
- **Коммиты:** `<тип>: <описание на русском>` (Conventional Commits)
- Типы: `feat`, `fix`, `refactor`, `docs`, `style`, `test`, `chore`

---

### 3.4. Локализация
- [ ] Документировать систему String Catalogs
- [ ] Описать правила добавления новых строк
- [ ] Привести примеры использования String(localized:)

**Зачем AI-агенту:** Понимание системы локализации позволяет агенту не хардкодить строки в UI.

- String Catalogs (`.xcstrings`)
- `String(localized:)` для всех UI строк
- Константы для чисел, цветов, размеров

---

## 4. Data Flow (Поток данных)

### 4.1. UI → ViewModel → Service
- [ ] Документировать паттерн ViewAction
- [ ] Описать flow обработки пользовательского ввода
- [ ] Привести примеры кода

**Зачем AI-агенту:** Понимание потока данных позволяет агенту правильно обрабатывать пользовательский ввод и обновлять UI.

```
User Input → View.send(action) → ViewModel.action → Service.calculate() → @Published → View
```

**Пример (SurebetCalculator):**
```swift
// View
TextField(...)
    .onChange(of: text) { viewModel.send(.setTextFieldText(field, text)) }

// ViewModel
func send(_ action: ViewAction) {
    switch action {
    case let .setTextFieldText(field, text):
        set(field, text: text)
        calculate()  // → calculationService.calculate(...)
    }
}
```

---

### 4.2. Network → Cache → UI (Banner)
- [ ] Документировать flow загрузки баннеров
- [ ] Описать стратегию кэширования
- [ ] Привести примеры работы с BannerService

**Зачем AI-агенту:** Понимание кэширования позволяет агенту правильно работать с сетевыми данными.

```
Banner.fetchBanner() → BannerService.fetchBannerAndImage() 
    → Network (URLSession) 
    → UserDefaults (banner JSON) + FileManager (image data)
    → Banner.isBannerFullyCached → UI
```

---

### 4.3. State Management
- [ ] Документировать все property wrappers
- [ ] Описать когда какой wrapper использовать
- [ ] Привести примеры использования

**Зачем AI-агенту:** Понимание управления состоянием позволяет агенту правильно использовать property wrappers.

| Wrapper | Назначение |
|---------|------------|
| `@Published` | Состояние ViewModel |
| `@StateObject` | Владение ViewModel в View |
| `@ObservedObject` | Наблюдение за ViewModel (не владение) |
| `@AppStorage` | Персистентное состояние (UserDefaults) |
| `@State` | Локальное состояние View |
| `@Binding` | Двусторонняя связь с родителем |

---

### 4.4. Analytics Flow
- [ ] Документировать систему аналитики
- [ ] Описать формат событий
- [ ] Привести список всех событий в приложении

**Зачем AI-агенту:** Понимание аналитики позволяет агенту добавлять трекинг событий в правильных местах.

```swift
analyticsService.log(name: "EventName", parameters: ["key": .bool(value)])
```

---

## 5. Testing (Тестирование)

### 5.1. Структура тестов
- [ ] Документировать структуру тестовых папок
- [ ] Описать паттерн создания моков
- [ ] Привести примеры тестов

**Зачем AI-агенту:** Понимание структуры тестов позволяет агенту писать тесты в правильном месте и стиле.

```
Tests/
├── ModuleNameTests/
│   ├── ModuleNameTests.swift
│   └── Mocks/
│       └── MockService.swift
```

---

### 5.2. Принципы тестирования
- [ ] Документировать правила написания тестов
- [ ] Описать особенности Swift 6 (MainActor, serialized)
- [ ] Привести примеры Given-When-Then

**Зачем AI-агенту:** Знание принципов позволяет агенту писать изолированные и надёжные тесты.

- Hand-written Mocks (без библиотек)
- `@MainActor` для тестовых классов с MainActor ViewModel
- `@Suite(.serialized)` для тестов с shared state (UserDefaults)
- Given-When-Then структура

---

## 📌 Приоритет документирования

| # | Раздел | Приоритет | Обоснование |
|---|--------|-----------|-------------|
| 1 | Модульная структура | 🔴 Высокий | Критично для навигации по коду |
| 2 | Swift 6 Concurrency | 🔴 Высокий | Частая причина ошибок |
| 3 | MVVM + DI | 🟡 Средний | Основа архитектуры |
| 4 | Git Conventions | 🟡 Средний | Нужно для коммитов |
| 5 | Data Flow | 🟡 Средний | Понимание бизнес-логики |
| 6 | Testing | 🟢 Низкий | Вторично к основному коду |

---

## 📝 Журнал изменений

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2026-01-25 | Документирован нейминг и код-стайл (3.2) | AI |
| 2026-01-25 | Документирована структура файлов (3.1) | AI |
| 2026-01-25 | Документирован Swift 6 Concurrency (2.3) | AI |
| 2026-01-25 | Документированы внешние зависимости (2.2) | AI |
| 2026-01-25 | Документированы версии и платформы (2.1) | AI |
| 2026-01-25 | Документирована навигация (1.4) | AI |
| 2026-01-25 | Документирован паттерн DI (1.3) | AI |
| 2026-01-25 | Документирован паттерн MVVM (1.2) | AI |
| 2026-01-25 | Создан Roadmap документации | AI |

---

## 🔗 Связанные документы

- [project_lessons.md](../project_lessons.md) — База знаний об ошибках и решениях
- [TEST_COVERAGE_ROADMAP.md](../TEST_COVERAGE_ROADMAP.md) — План покрытия тестами
- [README.md](../README.md) — Общая информация о проекте
