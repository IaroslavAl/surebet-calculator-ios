# Роадмап: Поддержка всех локализаций для ввода чисел

## 📋 Инструкция по работе с роадмапом

### Перед началом работы
1. **ОБЯЗАТЕЛЬНО** прочитай правила проекта:
   - `.cursor/rules/rules.mdc` - основные правила
   - `docs/rules/CODING_STANDARDS.md` - стандарты кода
   - `docs/rules/PROJECT_LESSONS.md` - база знаний об ошибках
   - `docs/SYSTEM_CONTEXT.md` - контекст проекта
   - `docs/architecture/DATA_FLOW.md` - архитектура
   - `docs/testing/TESTING_STRATEGY.md` - стратегия тестирования

2. **ОБЯЗАТЕЛЬНО** убедись, что находишься на правильной ветке:
   ```bash
   git branch --show-current
   # Должно быть: feature/multi-locale-numbers
   ```

### Процесс выполнения каждого пункта
1. **Прочитай пункт** - убедись, что понимаешь задачу
2. **Реализуй задачу** - выполни изменения в коде
3. **Проверь билд:**
   ```bash
   xcodebuild -project surebet-calculator.xcodeproj \
     -scheme surebet-calculator \
     -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9' \
     build
   ```
   Критерий успеха: `BUILD SUCCEEDED`, нет `error:` в выводе

4. **Проверь линтер:**
   - Используй `read_lints` для проверки ворнингов
   - Исправь все ворнинги (trailing whitespace, line length, nesting)

5. **Проверь тесты** (если изменялись тесты):
   ```bash
   xcodebuild test -project surebet-calculator.xcodeproj \
     -scheme surebet-calculator \
     -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9'
   ```
   Критерий успеха: `TEST SUCCEEDED` в выводе

6. **Создай коммит:**
   - Формат: `<тип>: <описание на русском>`
   - Пример: `feat: заменить хардкод ru_RU на Locale.current в String.swift`
   - Описание с маленькой буквы, без точки в конце, повелительное наклонение

7. **Если возникли ошибки:**
   - Исправь ошибки
   - Добавь запись в `docs/rules/PROJECT_LESSONS.md` в формате:
     - **Ошибка:** Краткое описание
     - **Причина:** Почему произошло
     - **Решение:** Как исправили
     - **Урок:** Правило, чтобы не повторить

### После завершения всех пунктов
1. Убедись, что все пункты выполнены
2. Проверь финальный билд и тесты
3. Создай финальный коммит (если остались изменения)
4. Готово к merge!

---

## 🗺️ Пункты роадмапа

### Пункт 1: Заменить хардкод ru_RU на Locale.current в String.swift
**Файл:** `SurebetCalculatorPackage/Sources/SurebetCalculator/Extensions/String.swift`

**Задача:**
- Заменить `Locale(identifier: "ru_RU")` на `Locale.current` в методе `formatToDouble()`
- Это позволит использовать локаль устройства пользователя для парсинга чисел

**Изменения:**
```swift
// Было:
formatter.locale = Locale(identifier: "ru_RU")

// Станет:
formatter.locale = Locale.current
```

**Проверка:**
- Билд должен проходить успешно
- Линтер не должен показывать ошибок

**Коммит:** `feat: использовать Locale.current для парсинга чисел в String.swift`

---

### Пункт 2: Заменить хардкод ru_RU на Locale.current в Double.swift
**Файл:** `SurebetCalculatorPackage/Sources/SurebetCalculator/Extensions/Double.swift`

**Задача:**
- Заменить `Locale(identifier: "ru_RU")` на `Locale.current` в методе `formatToString()`
- Это позволит использовать локаль устройства пользователя для форматирования чисел

**Изменения:**
```swift
// Было:
formatter.locale = Locale(identifier: "ru_RU")

// Станет:
formatter.locale = Locale.current
```

**Проверка:**
- Билд должен проходить успешно
- Линтер не должен показывать ошибок

**Коммит:** `feat: использовать Locale.current для форматирования чисел в Double.swift`

---

### Пункт 3: Обновить unit тесты для StringExtension - сделать их локализационно-независимыми
**Файл:** `SurebetCalculatorPackage/Tests/SurebetCalculatorTests/StringExtensionTests.swift`

**Задача:**
- Обновить тесты, чтобы они работали с любой локалью
- Использовать явную локаль в тестах через `Locale.current` или тестировать логику, а не конкретный формат
- Тесты, которые проверяют конкретный формат (запятая vs точка), нужно переписать:
  - Либо использовать `Locale.current` и проверять формат текущей локали
  - Либо тестировать логику парсинга, а не конкретный разделитель

**Стратегия:**
1. Тесты, которые проверяют валидность парсинга (например, `formatToDoubleWhenValidNumberWithComma`) - оставить, но использовать формат текущей локали
2. Тесты, которые проверяют конкретный формат (например, `formatToDoubleWhenLocalizationRuRu`) - переписать, чтобы проверять работу с текущей локалью
3. Тест `formatToDoubleWhenValidNumberWithPoint` - обновить: если текущая локаль использует точку, тест должен проходить

**Пример обновления:**
```swift
@Test
func formatToDoubleWhenValidNumberWithDecimalSeparator() {
    // Given
    let formatter = NumberFormatter()
    formatter.numberStyle = .decimal
    formatter.locale = Locale.current
    let testValue = 123.45
    let string = formatter.string(from: testValue as NSNumber) ?? ""
    
    // When
    let result = string.formatToDouble()
    
    // Then
    #expect(result == 123.45)
}
```

**Проверка:**
- Билд должен проходить успешно
- Тесты должны проходить успешно
- Линтер не должен показывать ошибок

**Коммит:** `test: обновить StringExtension тесты для поддержки всех локализаций`

---

### Пункт 4: Обновить unit тесты для DoubleExtension - сделать их локализационно-независимыми
**Файл:** `SurebetCalculatorPackage/Tests/SurebetCalculatorTests/DoubleExtensionTests.swift`

**Задача:**
- Обновить тесты, чтобы они работали с любой локалью
- Использовать `Locale.current` для проверки формата
- Тесты, которые проверяют конкретный формат (запятая), нужно переписать

**Стратегия:**
1. Тесты, которые проверяют конкретный формат (например, `formatToStringWhenLocalizationRuRu`) - переписать, чтобы проверять работу с текущей локалью
2. Тесты, которые проверяют логику (округление, отрицательные числа) - оставить, но использовать формат текущей локали для проверки

**Пример обновления:**
```swift
@Test
func formatToStringWhenPositiveNumber() {
    // Given
    let value: Double = 123.45
    let formatter = NumberFormatter()
    formatter.numberStyle = .none
    formatter.minimumFractionDigits = 0
    formatter.maximumFractionDigits = 2
    formatter.locale = Locale.current
    let expected = formatter.string(from: value as NSNumber) ?? ""
    
    // When
    let result = value.formatToString()
    
    // Then
    #expect(result == expected)
}
```

**Проверка:**
- Билд должен проходить успешно
- Тесты должны проходить успешно
- Линтер не должен показывать ошибок

**Коммит:** `test: обновить DoubleExtension тесты для поддержки всех локализаций`

---

### Пункт 5: Обновить UI тесты для поддержки всех локализаций
**Файл:** `SurebetCalculatorUITests/SurebetCalculatorUITests.swift`

**Задача:**
- Обновить UI тесты, которые используют хардкод запятую в вводе чисел
- Использовать формат текущей локали или явно устанавливать локаль для тестов

**Найденные места с хардкодом:**
- Строка 158: `row0Coefficient.typeText("2,1")` - использовать формат текущей локали
- Строка 162: `row1Coefficient.typeText("2,1")` - использовать формат текущей локали
- Строка 189: `row0Coefficient.typeText("2,5")` - использовать формат текущей локали

**Стратегия:**
1. Создать helper метод для форматирования числа в строку для ввода:
   ```swift
   private func formatNumberForInput(_ value: Double) -> String {
       let formatter = NumberFormatter()
       formatter.numberStyle = .decimal
       formatter.locale = Locale.current
       formatter.maximumFractionDigits = 2
       return formatter.string(from: value as NSNumber) ?? ""
   }
   ```

2. Заменить хардкод на использование этого метода:
   ```swift
   // Было:
   row0Coefficient.typeText("2,1")
   
   // Станет:
   row0Coefficient.typeText(formatNumberForInput(2.1))
   ```

**Проверка:**
- Билд должен проходить успешно
- UI тесты должны проходить успешно (запустить на симуляторе)
- Линтер не должен показывать ошибок

**Коммит:** `test: обновить UI тесты для поддержки всех локализаций`

---

### Пункт 6: Проверить логику расчетов и убедиться что ничего не сломалось
**Файлы для проверки:**
- `SurebetCalculatorPackage/Sources/SurebetCalculator/ViewModels/SurebetCalculatorViewModel.swift`
- `SurebetCalculatorPackage/Sources/SurebetCalculator/Calculator/CalculationService.swift`
- `SurebetCalculatorPackage/Sources/SurebetCalculator/Calculator/DefaultCalculationService.swift`
- `SurebetCalculatorPackage/Sources/SurebetCalculator/Calculator/Calculator.swift`

**Задача:**
- Убедиться, что логика расчетов не зависит от локали
- Проверить, что все места, где используется `formatToDouble()` и `formatToString()`, работают корректно
- Запустить все существующие тесты калькулятора

**Проверка:**
1. Запустить unit тесты калькулятора:
   ```bash
   xcodebuild test -project surebet-calculator.xcodeproj \
     -scheme surebet-calculator \
     -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9'
   ```

2. Проверить, что все тесты проходят

3. Если есть проблемы - исправить и добавить запись в `PROJECT_LESSONS.md`

**Коммит:** `chore: проверить логику расчетов после изменений локализации`

---

### Пункт 7: Финальная проверка билда и тестов
**Задача:**
- Выполнить полную проверку проекта

**Проверки:**
1. **Билд:**
   ```bash
   xcodebuild -project surebet-calculator.xcodeproj \
     -scheme surebet-calculator \
     -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9' \
     build
   ```
   Критерий: `BUILD SUCCEEDED`

2. **Линтер:**
   - Использовать `read_lints`
   - Исправить все ворнинги

3. **Unit тесты:**
   ```bash
   xcodebuild test -project surebet-calculator.xcodeproj \
     -scheme surebet-calculator \
     -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9'
   ```
   Критерий: `TEST SUCCEEDED`

4. **UI тесты (опционально, но рекомендуется):**
   - Запустить UI тесты вручную в Xcode или через команду
   - Убедиться, что все проходят

**Если все проверки пройдены:**
- Готово к merge!
- Можно создать PR

**Коммит:** Не требуется (если нет изменений)

---

## 📝 Заметки

### Важные моменты
- `Locale.current` использует локаль устройства пользователя
- NumberFormatter автоматически использует правильный разделитель (запятая или точка) в зависимости от локали
- Тесты должны быть локализационно-независимыми, но могут проверять работу с текущей локалью
- UI тесты должны использовать формат текущей локали для ввода чисел

### Потенциальные проблемы
- Если тесты падают из-за локали - нужно использовать явную локаль в тестах или тестировать логику, а не формат
- Если UI тесты падают - проверить, что используется правильный формат для ввода (запятая vs точка)

### Ссылки на документацию
- [Apple: NumberFormatter](https://developer.apple.com/documentation/foundation/numberformatter)
- [Apple: Locale](https://developer.apple.com/documentation/foundation/locale)
