# 📊 Роадмап переработки аналитики

> **Ветка:** `refactor/analytics-events-rework`
> **Дата создания:** 2026-01-25

---

## 📋 Текущее состояние

### Существующие события:

| Событие | Файл | Параметры | Проблемы |
|---------|------|-----------|----------|
| `RequestReview` | RootViewModel | `enjoying_calculator: Bool` | ✅ Нормально |
| `OpenedBanner(\(id))` | FullscreenBannerView | — | ⚠️ ID в названии события — плохая практика |
| `ClosedBanner(\(id))` | FullscreenBannerView | — | ⚠️ ID в названии события — плохая практика |
| `ClickingOnAnAdvertisement` | BannerView | — | ⚠️ Статический метод, нет DI |
| `CloseBanner` | BannerView | — | ⚠️ Статический метод, нет DI |

### Отсутствующие события:

| Событие | Где добавить | Описание |
|---------|--------------|----------|
| `OnboardingStarted` | OnboardingViewModel | Пользователь начал онбординг |
| `OnboardingCompleted` | OnboardingViewModel | Пользователь завершил онбординг |
| `OnboardingSkipped` | OnboardingViewModel | Пользователь пропустил онбординг |
| `OnboardingPageViewed` | OnboardingViewModel | Просмотр страницы онбординга |
| `CalculatorRowAdded` | SurebetCalculatorViewModel | Добавлена строка калькулятора |
| `CalculatorRowRemoved` | SurebetCalculatorViewModel | Удалена строка калькулятора |
| `CalculatorCleared` | SurebetCalculatorViewModel | Калькулятор очищен |
| `CalculationPerformed` | SurebetCalculatorViewModel | Выполнен расчёт |
| `AppOpened` | RootViewModel | Приложение открыто |

---

## 🎯 Целевая архитектура событий

### Принципы именования:
1. **snake_case** для названий событий
2. **Параметры** вместо динамических имён событий
3. **Категоризация** событий по модулям

### Новая структура событий:

```swift
// Категория: Onboarding
onboarding_started
onboarding_page_viewed { page_index: Int, page_title: String }
onboarding_completed { pages_viewed: Int }
onboarding_skipped { last_page_index: Int }

// Категория: Calculator
calculator_row_added { row_count: Int }
calculator_row_removed { row_count: Int }
calculator_cleared
calculation_performed { row_count: Int, profit_percentage: Double }

// Категория: Banner
banner_viewed { banner_id: String, banner_type: String }
banner_clicked { banner_id: String, banner_type: String }
banner_closed { banner_id: String, banner_type: String }

// Категория: Review
review_prompt_shown
review_response { enjoying_app: Bool }

// Категория: App
app_opened { session_number: Int }
```

---

## 🚀 Пошаговый план выполнения

> **ИНСТРУКЦИЯ ДЛЯ АГЕНТА:**
> 1. Выполняй каждый пункт полностью до перехода к следующему
> 2. После каждого пункта проверяй сборку: `xcodebuild -project surebet-calculator.xcodeproj -scheme surebet-calculator -destination 'id=F8F50881-5D0E-49DA-AA54-1312A752EED9' build`
> 3. Проверяй линтер: `read_lints` для изменённых файлов
> 4. Запускай тесты если они затронуты: `xcodebuild test -project surebet-calculator.xcodeproj -scheme surebet-calculator -testPlan surebet-calculator`
> 5. Создавай коммит только после успешной проверки
> 6. Формат коммита: `<тип>: <описание на русском>`

---

### Пункт 1: Создать типобезопасный каталог событий аналитики

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/AnalyticsManager/AnalyticsManager.swift`

**Задачи:**
1. Создать `enum AnalyticsEvent` с типобезопасными событиями
2. Добавить метод `log(event: AnalyticsEvent)` в `AnalyticsService`
3. Сохранить обратную совместимость со старым методом `log(name:parameters:)`

**Подпункты:**
- 1.1 Создать `AnalyticsEvent.swift` с enum событий
- 1.2 Обновить `AnalyticsService` протокол
- 1.3 Обновить `AnalyticsManager` реализацию

**Коммит:** `refactor: создать типобезопасный каталог событий аналитики`

---

### Пункт 2: Обновить события баннеров (FullscreenBannerView)

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/Banner/FullscreenBannerView.swift`

**Задачи:**
1. Заменить `OpenedBanner(\(id))` на `banner_clicked` с параметром `banner_id`
2. Заменить `ClosedBanner(\(id))` на `banner_closed` с параметром `banner_id`
3. Добавить событие `banner_viewed` при показе баннера

**Коммит:** `refactor: обновить события аналитики в FullscreenBannerView`

---

### Пункт 3: Обновить события баннеров (BannerView) и добавить DI

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/Banner/BannerView.swift`

**Задачи:**
1. Добавить DI для `AnalyticsService` вместо статического вызова
2. Заменить `ClickingOnAnAdvertisement` на `banner_clicked`
3. Заменить `CloseBanner` на `banner_closed`
4. Добавить параметр `banner_type: "inline"` для отличия от fullscreen

**Коммит:** `refactor: обновить события аналитики в BannerView и добавить DI`

---

### Пункт 4: Обновить события в RootViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/Root/RootViewModel.swift`

**Задачи:**
1. Заменить `RequestReview` на `review_response`
2. Добавить событие `review_prompt_shown` при показе алерта
3. Добавить событие `app_opened` с номером сессии

**Коммит:** `refactor: обновить события аналитики в RootViewModel`

---

### Пункт 5: Добавить аналитику в OnboardingViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/Onboarding/ViewModels/OnboardingViewModel.swift`

**Задачи:**
1. Добавить DI для `AnalyticsService`
2. Добавить событие `onboarding_started` при инициализации
3. Добавить событие `onboarding_page_viewed` при смене страницы
4. Добавить событие `onboarding_completed` при завершении
5. Добавить событие `onboarding_skipped` при пропуске

**Коммит:** `feat: добавить события аналитики в OnboardingViewModel`

---

### Пункт 6: Добавить аналитику в SurebetCalculatorViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Sources/SurebetCalculator/ViewModels/SurebetCalculatorViewModel.swift`

**Задачи:**
1. Добавить DI для `AnalyticsService`
2. Добавить событие `calculator_row_added` при добавлении строки
3. Добавить событие `calculator_row_removed` при удалении строки
4. Добавить событие `calculator_cleared` при очистке
5. Добавить событие `calculation_performed` при расчёте (с debounce)

**Коммит:** `feat: добавить события аналитики в SurebetCalculatorViewModel`

---

### Пункт 7: Обновить тесты AnalyticsManager

**Файлы для изменения:**
- `SurebetCalculatorPackage/Tests/AnalyticsManagerTests/AnalyticsManagerTests.swift`
- `SurebetCalculatorPackage/Tests/AnalyticsManagerTests/Mocks/MockAnalyticsService.swift`

**Задачи:**
1. Обновить `MockAnalyticsService` для поддержки новых событий
2. Добавить тесты для `AnalyticsEvent` enum
3. Добавить тесты для нового метода `log(event:)`

**Коммит:** `test: обновить тесты AnalyticsManager`

---

### Пункт 8: Обновить тесты RootViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Tests/RootTests/RootViewModelTests.swift`
- `SurebetCalculatorPackage/Tests/RootTests/Mocks/MockAnalyticsService.swift`

**Задачи:**
1. Обновить моки для новых событий
2. Обновить существующие тесты для новых названий событий
3. Добавить тесты для новых событий (`app_opened`, `review_prompt_shown`)

**Коммит:** `test: обновить тесты RootViewModel для новых событий аналитики`

---

### Пункт 9: Добавить тесты для OnboardingViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Tests/OnboardingTests/OnboardingViewModelTests.swift`

**Задачи:**
1. Добавить тесты для событий онбординга
2. Проверить корректность параметров событий

**Коммит:** `test: добавить тесты аналитики для OnboardingViewModel`

---

### Пункт 10: Добавить тесты для SurebetCalculatorViewModel

**Файлы для изменения:**
- `SurebetCalculatorPackage/Tests/SurebetCalculatorTests/SurebetCalculatorViewModelTests.swift`

**Задачи:**
1. Добавить тесты для событий калькулятора
2. Проверить корректность параметров событий

**Коммит:** `test: добавить тесты аналитики для SurebetCalculatorViewModel`

---

### Пункт 11: Обновить документацию

**Файлы для изменения:**
- `docs/architecture/DATA_FLOW.md`
- `docs/architecture/MODULES.md`
- `docs/SYSTEM_CONTEXT.md`

**Задачи:**
1. Обновить раздел "Analytics Flow" в DATA_FLOW.md
2. Обновить каталог событий
3. Добавить описание новой архитектуры событий

**Коммит:** `docs: обновить документацию аналитики`

---

### Пункт 12: Финальная проверка и удаление deprecated кода

**Файлы для изменения:**
- Все файлы с аналитикой

**Задачи:**
1. Удалить старый метод `log(name:parameters:)` если больше не используется
2. Удалить статический метод `AnalyticsManager.log()`
3. Проверить все импорты и зависимости
4. Запустить полный набор тестов

**Коммит:** `refactor: удалить deprecated методы аналитики`

---

## ✅ Чеклист завершения

- [ ] Все события используют типобезопасный enum
- [ ] Все компоненты используют DI для аналитики
- [ ] Нет статических вызовов `AnalyticsManager.log()`
- [ ] Все тесты проходят
- [ ] Нет ворнингов линтера
- [ ] Документация обновлена
- [ ] Проект собирается без ошибок

---

## 📝 Примечания

- При добавлении аналитики в калькулятор использовать debounce для `calculation_performed`, чтобы не спамить события при каждом нажатии клавиши
- События баннеров должны различать `fullscreen` и `inline` типы
- Номер сессии (`session_number`) берётся из `numberOfOpenings` в RootViewModel
